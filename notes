variable for cost
variable for ttcost

add name
	create variable for namecost
	create variable for ttnamecost

function for update costs
	run every time inputs are updated

	calculate:
		total cost
		tip
		complete cost
		each individual cost
			cost + (cost/totalCost)*tax + tip*cost
	
